[package]
name = "agentshell"
version = "0.1.0"
description = "多平台 Token 壳子桌面 Agent"
edition = "2021"
rust-version = "1.70"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
opt-level = "s"
lto = true
